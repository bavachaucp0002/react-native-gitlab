export default interface AppLanguage {
  common: {
    empty: string
  }
  error: {
    userNotFound: string
    unauthorized: string
  }
  repositories: {
    title: string
  }
  issues: {
    title: string
  }
  merge_request: {
    title: string
  }
  todo: {
    title: string
  }
  account: {
    title: string
  }
}
